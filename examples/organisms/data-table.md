# Data Table Organism

The data table is the default repeated-record surface.

Rules:
- headers align to cell content
- sortable columns expose sort direction
- row actions stay predictable and compact
- selected, hover, empty, loading, and error states are required
- pagination must not shift the table height unexpectedly
