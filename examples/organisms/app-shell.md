# App Shell Organism

The app shell owns primary navigation, workspace context, account controls, and global page structure.

Rules:
- navigation state must be visible
- active item uses accent treatment and semantic state, not decorative color
- content region should keep stable gutters across pages
- mobile layout must avoid overlapping navigation and page actions
