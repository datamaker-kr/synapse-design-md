# Action Group Molecule

Action groups collect related commands.

Rules:
- use one primary action per group
- icon-only actions require accessible names
- destructive actions must not sit beside routine primary actions without separation
