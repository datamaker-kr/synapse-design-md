# Create/Edit Form Template

Use this template for create and edit flows.

Required regions:
- page title
- grouped form sections
- inline validation
- sticky or clearly placed submit/cancel actions
- success and error feedback

Design notes:
- labels must stay close to fields
- validation messages must name the failing field
- long forms should use sections, not nested cards
- disabled and loading submit states must be explicit
